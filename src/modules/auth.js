import AuthServise from "../service/auth";

const state = {
      isLoading : false,
}
const actions ={
      register(context){
            
      }
}
const mutations = {
      setLoading(state){
            state.isLoading = true
      }
}
export default {state, mutations , actions}